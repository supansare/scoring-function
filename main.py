import os
from ibm_watson_machine_learning import APIClient

def main(params):

  return {
        "headers": {
            "Content-Type": "application/json",
        },
        "statusCode": 200,
        "body": "hello",
  }
